请根据main_workflow_v2的代码逻辑，按要求完成研究生学术论文第3章的撰写：

**论文写作风格：**分段式表达，

1. 逻辑清晰，层次分明  
2. 适合硕士/博士论文  
3. 易读性强。



**我们的场景是：**

我们在做基于容器逃逸的Python代码静态检测，该方法采用双通道并行特征提取架构，通过语义特征提取和结构特征提取两大模块，一方面通过抽象语法树（AST）分析和模式匹配提取128维语义特征，涵盖敏感函数调用、攻击模式、路径配置和混淆检测四个维度；另一方面，通过构建控制流图（CFG）和函数调用图（FCG），利用层次化图神经网络（GNN）学习代码的结构特征，生成128维结构嵌入向量。后期将两类特征拼接到一起，再输入下游的模型做监督学习的二分类任务（恶意程序检测）



## 第3章节标题：静态分析：基于容器逃逸的Python代码检测

### 3.1 语义特征提取方法

3.1小节的内容已经撰写完成，无需你撰写，你只负责撰写3.2章节的内容。

### 3.2 结构特征提取方法

### 3.2.1 解析和构建图

将代码语义转换为图结构，这是GNN的直接输入形式。

这一小节需要你详细介绍控制流图和函数调用图的构建、从原理到实现都要涉及到，请自己构思文章的叙述逻辑。

**注意：**构建cfg图和fcg图的具体实现，涉及到的核心代码逻辑需要给出伪代码形式的算法实现；涉及到的相关的数学逻辑需要进行解释，并给出具体的数学公式。

#### （1）控制流程图构建

一个函数对应一个cfg图，CFG的节点（表示特定控制语句的ast节点）和边（表示控制流）
cfg.nodes属性信息，需要给我一个如下类似的通用字典范式： 
`CFG 节点属性字典（cfg.nodes[node_id]）
{
    'node_type': str,                    # 节点类型
    'code_snippet_raw_text': str,        # 代码片段原始文本
    'has_conditional_branch': bool,      # 是否有条件分支
    'lineno': int,                       # 源代码行号
    'is_sensitive_api': bool,            # 是否为敏感API调用
    'sensitive_pattern': str,            # 敏感API模式名称
    'complexity': int,                   # 圈复杂度
    'feature_vector': np.ndarray,        # 节点特征向量（256维）
}
CFG 边属性字典（cfg.edges[u, v]）
{
    'edge_type': str,           # 边类型
    'is_back_edge': bool,       # 是否为回边（可选）
}`
**重点介绍：**为CFG节点添加特征向量（使用CodeBERT文本嵌入，使用线性投影降维）的具体实现，涉及到的核心代码逻辑需要给出伪代码形式的算法实现。

针对我的任务场景：通过控制语句代码片段生成文本嵌入特征，做为cfg节点特征。
分别选择：CodeBERTa-small、CodeBERT-MLM、GraphCodeBERT、CodeBERT-base做文本嵌入预训练模型。
首先简要介绍这几个模型，对比这几个模型的特点与核心优势，给出你之所以选择这几个模型做文本嵌入的理由依据（比如：具备优秀的代码理解能力、最好具备程序控制流理解能力、适合处理短代码片段）
最后要设计对比实验，针对短代码片段任务生成模型对比表，评估指标比如包含:1.性能指标（Accuracy、F1-score、AUC-ROC、Precision、Recall）、2.效率指标：单样本推理时间（ms）批量推理吞吐量（samples/s）、内存占用（MB）、特征提取总时间（分钟））

#### （2）函数调用图构建

一个模块对应一个fcg图，FCG的节点（表示函数）和边（表示调用关系）
fcg.nodes属性信息，需要给我一个如下类似的通用字典范式：    
`FCG 节点属性字典（fcg.nodes[func_name]）
{
    'function_name': str,           # 函数名称
    'name': str,                    # 函数名称（冗余，与function_name相同）
    'args_count': int,              # 位置参数数量
    'defaults_count': int,          # 默认参数数量
    'kw_only_count': int,           # 仅关键字参数数量
    'has_var_args': bool,           # 是否有*args
    'has_var_kwargs': bool,         # 是否有**kwargs
    'body_length': int,             # 函数体语句数量
    'decorator_count': int,         # 装饰器数量
    'returns_count': int,           # return语句数量
    'docstring_length': int,        # 文档字符串长度
    'has_sensitive_api': bool,      # 是否包含敏感API调用
    'sensitive_api_count': int,     # 敏感API调用次数
}
FCG 边属性字典（fcg.edges[caller, callee]）
{
    'call_count': int,      # 调用次数
    'lineno': int,          # 首次调用的行号
}`
这些属性为 GNN 模型提供了丰富的特征信息，支持容器逃逸检测等安全分析任务。

**注意：**另外后面更好的进行自监督边预测任务，在构建图的过程中可以添加一些针对图数据构建的后处理方法：

比如，通过一些方法过裁剪掉图中一些无意义的节点，这些具体的方法需要你自己发挥和设计，添加到论文对应部分，要求逻辑清楚、思路严谨，需要带上对应的算法伪代码实现或数学公式说明。

本小节对应代码实现请参考：graph_builder_v2.py

### 3.2.2 图表示学习任务

首先简要介绍图表示学习的概念，说明我们之所以选择图表示学习的理由与好处，然后回到我们的任务中简练概括相关部分的核心实现逻辑，最后补充论据：经过实验效果不错。

#### （1）数据预处理

首先，先介绍图数据对象的构建，将cfg和fcg图对象转换为PyG格式。将`networkx`图对象及其包含的特征转换为特定GNN框架（PyTorch Geometric）的数据对象所需的格式。

然后，按文件划分数据集（避免数据泄漏）,这些都属于训练前的数据预处理部分，需要简要介绍即可。

这部分你可以自由发挥。

#### （2）自监督边预测任务

接下来进入本节重点：
**阶段1:** 训练CFG_GNN模型（自监督边预测任务），这个阶段需要参考CFGGNN_V2类的代码将cfg_gnn模型架构草图画出来，并加以说明。

1. 训练阶段为边预测任务（自监督），最后得到函数级的图级嵌入。
2. 接着，提取所有CFG的图级嵌入，并注入CFG嵌入到FCG节点特征。

**注意：**要详细介绍自监督边预测任务，这是本小节的核心内容。

**要说明：**

1. 在我们的任务中之所以选择自监督边预测任务的理由依据，有什么好处？
2. 模型训练过程中的自监督的边预测任务具体过程逻辑是如何实现的？
3. 涉及到的核心代码逻辑需要给出伪代码形式的算法实现。
4. 我们的自监督边预测任务中用点积表示边的得分意义是什么？给出涉及到的相关数学公式并加以说明。

**阶段2:** 训练FCG GNN模型（自监督边预测任务），这个阶段需要参考FCGGGNN_V2类的代码将fcg_gnn模型架构草图画出来，并加以说明。
**注意：**由于训练阶段也为边预测任务（自监督），此处内容会与阶段1重复，重复的内容简要概括即可，注意避免重复啰嗦，重在论文的逻辑通顺严谨即可。
最后得到程序级的图级嵌入。

**注意：**
cfg_gnn模型架构草图包括：
1.输入层:GCNConv+BatchNorm1d+relu+dropout
2.隐藏层2层（带残差连接）:（GCNConv+BatchNorm1d+relu+dropout+残差连接 → GCNConv+BatchNorm1d+relu+dropout）
3.输出层：GCNConv+BatchNorm1d
4.最后是AttentionPooling层

fcg_gnn模型架构草图包括：
1.输入层:GATConv+BatchNorm1d+elu+dropout
2.隐藏层2层（带残差连接）:（GATConv+BatchNorm1d+elu+dropout+残差连接 → GATConv+BatchNorm1d+elu+dropout）
3.输出层：GATConv+BatchNorm1d
4.最后是AttentionPooling层



#### （3）程序的图级嵌入

这部分可以详细补充介绍一下"注意力池化层"的自定义实现，具体代码请见gnn_trainer_v2.AttentionPooling类的实现，涉及到的核心代码逻辑需要给出伪代码形式的算法实现，并做简要说明。