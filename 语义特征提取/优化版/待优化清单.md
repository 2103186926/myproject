### 背景：

现在我的新需求是为了检测容器逃逸行为的python恶意程序，从而提取其对应的语义特征向量，虽然逻辑相同，但是具体的特征提取实现方式需要重新设计了。请帮我设计一套具备抗混淆增强的并能检测容器逃逸行为的python恶意程序的语义特征提取方案。

## ✅ 优化完成状态

**优化版本**: `semantic_feature_extractor_v3.py`
**优化日期**: 2026年1月14日
**测试状态**: 通过

下面是我在debug调试过程中发现的问题，请你给出具体的优化方案：

* 1.功能参数优化

* 增加一个可选参数的选项，功能：将输入源码的ast解析后的json格式保存下来。如果选择该参数，可以进一步给出保存路径。
* 增加一个可选参数的选项，功能：可视化抽象语法树（AST），使用graphviz库实现。如果选择该参数，可以进一步给出PNG文件保存路径。

**✅ 已完成**：

```bash
# 保存AST JSON
python semantic_feature_extractor_v3.py sample.py --save-ast
python semantic_feature_extractor_v3.py sample.py --save-ast --ast-path output.ast.json

# 生成AST可视化图
python semantic_feature_extractor_v3.py sample.py --visualize-ast
python semantic_feature_extractor_v3.py sample.py --visualize-ast --ast-png output.ast
```

### 2.可行性待验证的地方：

在全局变量中，像CONTAINER_ESCAPE_MODULES、CONTAINER_ESCAPE_PATTERNS、ESCAPE_RISK_RULES等都是基于《检测规则手册》，但是检测规则手册的内容可靠吗？全面吗？请审核《检测规则手册》的内容并进行评估，并且重新优化文档内容，添加更多容器逃逸漏洞的检测规则（如最新的CVE），请永远记住我的终极目标是为了做容器逃逸静态代码检测，前提的规则驱动是：要有一个权威的全面的《Python容器逃逸静态检测规则手册》的标准化规则。

**✅ 已完成**：

- 新增CVE检测规则：CVE-2022-0185 (fsconfig)、CVE-2022-0492 (cgroup v1)、CVE-2021-22555 (Netfilter)、CVE-2020-15257 (containerd-shim)
- 扩展敏感模块：从原来的13个模块扩展到30+个模块
- 扩展攻击模式：从原来的30个模式扩展到50+个模式
- 完善风险等级：增加LOW级别，细化CRITICAL/HIGH/MEDIUM分类

### 3.debug过程中产生的疑问请解决

**1.检测代码的容器逃逸风险的过程中**

* （1）定义容器逃逸攻击模式（基于检测规则手册5.2节）时，CONTAINER_ESCAPE_PATTERNS字典包含的容器逃逸攻击模式全面吗？可靠吗？还存在什么优化或可补充的吗？
* （2）定义检测规则的风险等级（对应检测规则手册5.2节）时，ESCAPE_RISK_RULES对风险等级的划分合理吗？是否做过充分调研？

最后，要给我提供一份完善的《Python容器逃逸静态检测规则文档》，在该文档中要列出一份CONTAINER_ESCAPE_PATTERNS中所有攻击模式的详细信息清单。

**✅ 已完成**：

- 攻击模式扩展至50+个，覆盖8大类别（CMD/FILE/LIB/NET/EXPLOIT/K8S/CRED/DOS）
- 风险等级细化为4级（CRITICAL/HIGH/MEDIUM/LOW），基于CVSS评分和实际攻击影响
- 完整文档已创建：《Python容器逃逸静态检测规则文档_v2.md》

**2.检测代码混淆的过程中**

* （1）定义检测混淆特征obfuscation_indicators字典时，这些混淆特征的选取可靠吗？还存在什么可补充或可优化的吗？（比如：只检测eval/exec就行了吗，动态执行类不是还有其他的吗）
* （2）判断是否混淆时，如果满足>=2条obfuscation_indicators中的特征，就判为yes的逻辑可行吗？

**请特别注意**：我们检测代码混淆的目的是为了发现代码中是否对容器逃逸行为的高危关键字加了混淆，而不是为了检测代码是否使用了混淆技术。换句话说：不是不让用户使用混淆，而是要检测用户的目的是否是为了使用编码混淆来绕过detect_container_escape_risk的关键词检测（检测的第一关），
比如：

```
# 攻击者技巧：将敏感路径 base64 编码，绕过静态文本扫描
    # L3Zhci9ydW4vZG9ja2VyLnNvY2s=  -> /var/run/docker.sock
    # L3Byb2Mva2NvcmU=              -> /proc/kcore
    # L2hvbWUvYWRtaW4=              -> /home/admin
```

**✅ 已完成**：

- 新增 `detect_obfuscation_bypass()` 方法，专门检测针对容器逃逸关键词的编码绕过
- 定义 `ESCAPE_SENSITIVE_KEYWORDS` 敏感关键词列表
- 实现多种解码尝试（Base64/Hex/Unicode/ROT13）
- 混淆检测扩展至14种模式（OBFUSCATION_PATTERNS）
- 混淆风险等级判定：基于解码出的敏感关键词数量和混淆技术数量

**3.提取语义特征向量的过程中：**
**（1）容器逃逸敏感函数频次特征向量**
a. 定义容器逃逸函数类别时，categories列表中的分类可靠吗？还存在什么可补充或可优化的吗？
b. BUILTIN_SENSITIVE内置敏感函数定义和CONTAINER_ESCAPE_MODULES容器逃逸相关的敏感模块和函数定义的内容可靠吗？全面了吗？
c. 特征的设计合理吗？这些特征能否抓到容器逃逸恶意行为？是否存在完美分类边界？需要审核并优化

**（2）提取容器逃逸攻击模式特征**
a. pattern_categories分类可靠吗？还存在什么可补充或可优化的吗？

**（3）提取容器逃逸路径和配置特征**
a. 敏感路径类别path_categories的定义可靠吗？还存在什么可补充或可优化的吗？

**✅ 已完成**：

- 敏感函数类别扩展至8类（system_commands/process_control/dangerous_libs/network_ops/file_ops/dynamic_exec/encoding_ops/serialization）
- CONTAINER_ESCAPE_MODULES扩展至30+个模块
- 攻击模式类别扩展至12类
- 路径配置类别扩展至6类
- 每类提取4个特征（存在性/对数频次/相对频率/归一化频次），避免完美分类边界

**4.优化特征维度的设计**
特征维度设计的合理吗？目前设计的特征提取方案能否抓到容器逃逸恶意行为？是否存在完美分类边界？需要你再次审核并优化，改进特征工程，移除或改造“完美特征”。

**✅ 已完成**：

- 特征维度重构为128维（32敏感函数 + 48攻击模式 + 24路径配置 + 24混淆检测）
- 每类使用4个特征（存在性/对数频次/多样性/归一化频次），避免单一特征导致的完美分类
- 使用对数变换和归一化处理，增强泛化能力
- 新增混淆检测特征维度，提升抗混淆能力

---

## ✅ 全部优化已完成

**输出文件**：

1. `semantic_feature_extractor_v3.py` - 优化版特征提取工具
2. `Python容器逃逸静态检测规则文档_v2.md` - 完整检测规则文档（含50+攻击模式详细清单）
