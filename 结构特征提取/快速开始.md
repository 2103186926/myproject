可选（可大大节约时间）：
--save_graphs 参数：保存 all_cfgs 和 all_fcgs 到磁盘
--load_graphs 参数：从磁盘加载已保存的图数据

--skip_training 参数：跳过cfg_gnn和fcg_gnn的训练过程，直接用训练好的模型进行推理
--cfg_model_path '必须预训练的CFG模型路径'
--fcg_model_path '必须预训练的FCG模型路径'

方式1：自监督预训练（边预测任务）----返回fcg_gnn的图级嵌入
(DL) D:\GitHub\CDK-main\CDK-main>
python main_workflow_v2.py 
--source_dir .\test_cases_v3\ 
--save_best_model 
--debug 

方式2：自监督预训练 + 微调 ---- 程序级的图分类任务（例如恶意/正常二分类）
(DL) D:\GitHub\CDK-main\CDK-main\结构特征提取>
python main_workflow_v2.py 
--source_dir .\test_cases_v3\ 
--mode train_classifier 
--label_file .\test_cases_v3\labels.csv 
--save_best_model 
--debug 

（仅评估分类器）
还未测试该参数......





# 快速开始指南

## 5分钟快速体验

### 步骤1：检查环境

```bash
# 确保已安装Python 3.8+
python --version

# 检查依赖（会自动检查所有必需的包）
python run_tests.py --skip-train --only-generate
```

### 步骤2：生成测试数据

```bash
# 生成海洋科学场景的测试用例
python test_case_generator.py
```

这将创建：
- `./test_cases/normal/` - 3个正常代码
- `./test_cases/malicious/` - 3个恶意代码
- `./test_cases/labels.csv` - 标签文件

### 步骤3：运行训练

```bash
# 使用默认配置训练
python main_workflow_v2.py \
    --source_dir ./test_cases \
    --label_file ./test_cases/labels.csv
```

### 步骤4：查看结果

```bash
# 查看输出文件
ls ./output/

# 查看训练日志
cat ./output/training.log

# 查看评估结果
cat ./output/evaluation_results.json
```

## 一键运行所有测试

```bash
# 运行完整的集成测试（包括依赖检查、数据生成、训练、评估）
python run_tests.py
```

## 自定义配置

### 1. 生成配置文件

```bash
python -c "from config import save_default_config; save_default_config('./my_config.json')"
```

### 2. 编辑配置文件

编辑 `my_config.json`，修改你想要的参数：

```json
{
  "model": {
    "cfg_num_layers": 4,
    "cfg_out_channels": 128,
    "fcg_num_layers": 3,
    "fcg_out_channels": 128
  },
  "training": {
    "cfg_epochs": 100,
    "fcg_epochs": 80,
    "batch_size": 32,
    "learning_rate": 0.001
  }
}
```

### 3. 使用自定义配置训练

```bash
python main_workflow_v2.py \
    --source_dir ./test_cases \
    --label_file ./test_cases/labels.csv \
    --config ./my_config.json \
    --output_dir ./my_output
```

## 生成框架图

```bash
# 根据配置自动生成GNN框架图
python generate_dynamic_framework_diagram.py
```

输出：
- `层次化GNN模型框架图_动态生成.png`
- `层次化GNN模型框架图_动态生成.pdf`

## 常用命令

### 仅生成测试用例
```bash
python run_tests.py --only-generate
```

### 快速训练（减少轮数）
```bash
python run_tests.py --quick
```

### 跳过依赖检查
```bash
python run_tests.py --skip-deps
```

### 调试模式
```bash
python main_workflow_v2.py \
    --source_dir ./test_cases \
    --label_file ./test_cases/labels.csv \
    --debug
```

## 使用自己的数据

### 1. 准备数据

创建你的Python代码目录：
```
my_data/
├── normal/
│   ├── code1.py
│   ├── code2.py
│   └── ...
└── malicious/
    ├── malware1.py
    ├── malware2.py
    └── ...
```

### 2. 创建标签文件

创建 `my_data/labels.csv`：
```csv
file_path,label
my_data/normal/code1.py,0
my_data/normal/code2.py,0
my_data/malicious/malware1.py,1
my_data/malicious/malware2.py,1
```

### 3. 运行训练

```bash
python main_workflow_v2.py \
    --source_dir ./my_data \
    --label_file ./my_data/labels.csv \
    --output_dir ./my_results
```

## 查看帮助

```bash
# 查看所有命令行参数
python main_workflow_v2.py --help

# 查看测试脚本参数
python run_tests.py --help
```

## 输出文件说明

训练完成后，`./output/` 目录包含：

| 文件 | 说明 |
|------|------|
| `cfg_gnn_model.pth` | CFG-GNN模型权重 |
| `fcg_gnn_model.pth` | FCG-GNN模型权重 |
| `training_history.json` | 训练历史（损失曲线数据） |
| `config.json` | 使用的配置参数 |
| `training.log` | 详细的训练日志 |
| `confusion_matrix.png` | 混淆矩阵图 |
| `roc_curve.png` | ROC曲线图 |
| `evaluation_results.json` | 评估指标（准确率、F1等） |

## 故障排除

### 问题1：显存不足

**解决方案**：
```bash
# 减小批次大小
python main_workflow_v2.py \
    --source_dir ./test_cases \
    --label_file ./test_cases/labels.csv \
    --config ./config.json  # 在配置中设置 batch_size: 16
```

### 问题2：训练太慢

**解决方案**：
```bash
# 使用快速模式
python run_tests.py --quick

# 或减少训练轮数（修改配置文件）
```

### 问题3：依赖缺失

**解决方案**：
```bash
# 安装所有依赖
pip install torch torch-geometric networkx numpy pandas matplotlib seaborn scikit-learn transformers tqdm
```

### 问题4：CodeBERT加载失败

**解决方案**：
在 `config.py` 中设置 `use_codebert: false`，将使用简单的哈希嵌入替代。

## 下一步

1. 阅读 `README_V2.md` 了解详细文档
2. 查看 `完成总结.md` 了解项目改进
3. 尝试修改配置参数优化性能
4. 使用自己的数据集进行训练

## 获取帮助

如有问题，请：
1. 查看 `README_V2.md` 的"常见问题"部分
2. 检查 `training.log` 日志文件
3. 提交Issue或联系开发者

---

**祝使用愉快！** 🎉
