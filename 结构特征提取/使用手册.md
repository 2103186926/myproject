main_workflow.py 使用指南
main_workflow.py 是一个用于提取Python代码结构特征并生成图嵌入向量的综合工具。以下是使用示例：

## 基本用法
最基本的使用方式是指定源代码目录和输出目录：
python structural_features/main_workflow.py --source_dir 路径/到/python/代码 --output_dir 输出目录

## 常用参数示例
1.指定训练参数
python structural_features/main_workflow.py \
  --source_dir 路径/到/python/代码 \
  --output_dir 输出目录 \
  --cfg_epochs 150 \
  --fcg_epochs 80 \
  --batch_size 64 \
  --learning_rate 0.0005 \
  --hidden_channels 256

2.控制图大小
python structural_features/main_workflow.py \
  --source_dir 路径/到/python/代码 \
  --output_dir 输出目录 \
  --min_nodes 3 \
  --max_nodes 1000

3.使用特定GPU
python structural_features/main_workflow.py \
  --source_dir 路径/到/python/代码 \
  --output_dir 输出目录 \
  --gpu_id 0  # 使用第一个GPU，-1表示使用CPU

4.跳过某些处理步骤
python structural_features/main_workflow.py \
  --source_dir 路径/到/python/代码 \
  --output_dir 输出目录 \
  --skip_training \
  --skip_embedding

5.启用调试模式
python structural_features/main_workflow.py \
  --source_dir 路径/到/python/代码 \
  --output_dir 输出目录 \
  --debug

6.指定日志文件位置
python structural_features/main_workflow.py \
  --source_dir 路径/到/python/代码 \
  --output_dir 输出目录 \
  --log_file 日志路径/log.txt

## 完整示例
python main_workflow.py
  --source_dir D:\python_project\DL_project\project03\high_risk_tests
  --output_dir D:\python_project\DL_project\project03\project_gnn\output\test
  --cfg_epochs 100
  --fcg_epochs 50
  --batch_size 32
  --learning_rate 0.001
  --hidden_channels 128
  --out_channels 64
  --num_layers 3
  --cpu_count 4
  --gpu_id 0
  --log_file D:\python_project\DL_project\project03\project_gnn\output\embedding_process.log


必需参数:
--source_dir      Python源代码目录

可选参数:
  --output_dir      输出目录 (默认: "output")
  --log_file        日志文件路径
  --min_nodes       图中最小节点数量 (默认: 5)
  --max_nodes       图中最大节点数量 (默认: 500)
  --cfg_epochs      CFG GNN训练轮数 (默认: 100)
  --fcg_epochs      FCG GNN训练轮数 (默认: 50)
  --batch_size      批处理大小 (默认: 32)
  --learning_rate   学习率 (默认: 0.001)
  --hidden_channels GNN隐藏层通道数 (默认: 128)
  --out_channels    GNN输出层通道数/嵌入维度 (默认: 64)
  --num_layers      GNN层数 (默认: 3)
  --dropout         Dropout率 (默认: 0.2)
  --cpu_count       要使用的CPU核心数
  --gpu_id          要使用的GPU ID，-1表示使用CPU (默认: 0)
  --seed            随机种子 (默认: 42)
  
  --skip_parsing    跳过解析步骤
  --skip_feature_extraction  跳过特征提取步骤
  --skip_graph_building      跳过图构建步骤
  --skip_training   跳过训练步骤
  --skip_embedding  跳过嵌入生成步骤
  --debug           启用调试模式

main_workflow工作流输出
脚本将在指定的输出目录中生成以下内容：
models/ - 保存训练好的GNN模型
cfg_gnn_model.pt - CFG GNN模型
fcg_gnn_model.pt - FCG GNN模型
embeddings/ - 保存生成的嵌入向量
{program_id}_embedding.npy - 每个程序的嵌入向量
training_metrics.json - 训练过程中的损失曲线
experiment_metadata.json - 实验元数据


## 简单使用
python structural_features/main_workflow.py --source_dir ".\high_risk_tests\" --output_dir ".\structural_features\output" --cfg_epochs 5 --fcg_epochs 3 --debug

python structural_features/main_workflow.py --source_dir ".\high_risk_tests\" --output_dir ".\structural_features\output" --debug > debug_output.log

## 详细使用
python structural_features/main_workflow.py --source_dir ".\high_risk_tests\" --output_dir  ".\structural_features\output" --cfg_epochs 100 --fcg_epochs 50 --batch_size 32 --learning_rate 0.001 --hidden_channels 128 --out_channels 64 --gpu_id 0 --debug

## 最新使用(main_workflow模型训练 + model_apply模型应用)
(DL) PS D:\python_project\DL_project\project04> python structural_features/main_workflow.py --source_dir ".\structural_features\dataset\" --label_file ".\structural_features\labels.csv"  --output_dir ".\structural_features\output" --cfg_epochs 5 --fcg_epochs 3 --out_channels 256 --debug 

(DL) PS D:\python_project\DL_project\project04> python .\structural_features\model_apply.py --source_dir .\structural_features\dataset\ --cfg_model_path .\structural_features\output\models\cfg_gnn_model.pt --fcg_model_path .\structural_features\output\models\fcg_gnn_model.pt --gpu_id 0